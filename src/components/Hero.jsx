const Hero = ({ className }) => {
    return (
      <section className={`${className} zone-1 bg-transparent text-white h-screen flex items-center justify-center`}>
      </section>
    );
  };
  
  export default Hero;